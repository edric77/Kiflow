# KIFLOW — site vitrine

Site statique (HTML / CSS / JS, sans framework) pour KIFLOW EI. Aucune étape de build
n'est nécessaire : c'est un site 100 % statique, donc le déploiement est immédiat.

## Fichiers
